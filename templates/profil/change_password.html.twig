{% extends 'base.html.twig' %}

{% block title %}Changer le mot de passe
{% endblock %}

{% block body %}

<div class="container mt-5 text-center">
    <h1>Modier mon mot de passe </h1>

    <div class="w-50 m-auto mt-5">
    {{ form_start(form) }}
        {{ form_row(form.currentPassword) }}
        {{ form_row(form.password) }}
        <button type ="submit" class="btn btn-primary">Modfier</button>
    {{ form_end(form) }}

    </div>


</div>
    {% endblock %}
    {#Si l'user n'est pas connecté -> app.user sera "null" , et donc app.user.prenom/email/nom n'existent pas#}
